name: defaultable_generator
description: The generator for the defaultable package.
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  defaultable: ^1.0.0

  build: ^2.4.1
  source_gen: ^1.5.0
  analyzer: '>=6.0.0 <7.0.0'
  recase: ^4.1.0

dev_dependencies:
  build_test: ^2.2.0
  test: ^1.24.0